working on validateToken
jwt service validatetoken returns 
return { valid: true, userInfo: decoded };

//here we use getProfile  //controller and validateAuthUser will do all the work

// for qr code generator
npm i uuid